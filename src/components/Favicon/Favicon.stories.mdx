import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Favicon from './Favicon';

<Meta
  title="Base Components/Favicon"
  argTypes={{
    rootDomain: {
      name: 'Domain',
      description: 'The domain',
      defaultValue: 'google.com',
      control: {
        type: 'text',
      },
    },
  }}
  component={Favicon}
/>

# Favicon

This component can add visualization to domains and makes it easier for the user to recognize them.

export const Template = (args) => <Favicon {...args} />;

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
  <Story name="Examples">
    <Favicon rootDomain={'physicsworld.com'} />
    <Favicon rootDomain={'bbc.co.uk'} />
    <Favicon rootDomain={'politifact.com'} />
    <Favicon rootDomain={'fullfact.org'} />
    <Favicon rootDomain={'usatoday.com'} />
    <Favicon rootDomain={'factchecker.in'} />
    <Favicon rootDomain={'verafiles.org'} />
    <Favicon rootDomain={'newsmobile.in'} />
    <Favicon rootDomain={'faktisk.no'} />
    <Favicon rootDomain={'verifica.efe.com'} />
  </Story>
</Canvas>

## Args Table

<ArgsTable of={Favicon} />
