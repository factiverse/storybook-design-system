import { ArgsTable, Canvas, Meta, Story } from '@storybook/addon-docs';

import Favicon from './Favicon';

<Meta
  title="Base Components/Favicon"
  argTypes={{
    domain: {
      name: 'Domain',
      description: 'The domain',
      defaultValue: 'google.com',
      control: {
        type: 'text',
      },
    },
  }}
  component={Favicon}
/>

# Favicon

This component can add visualization to domains and makes it easier for the user to recognize them.

export const Template = (args) => <Favicon {...args} />;

<Canvas>
  <Story name="Controls">{Template.bind({})}</Story>
  <Story name="Examples">
    <p>Favicons saved at the standard location domain.com/favicon.ico:</p>
    <Favicon domain={'physicsworld.com'} />
    <Favicon domain={'bbc.co.uk'} />
    <Favicon domain={'politifact.com'} />
    <Favicon domain={'fullfact.org'} />
    <p>
      Favicons referenced within head with rel="shortcute icon" or rel="icon":
    </p>
    <Favicon domain={'usatoday.com'} />
    <Favicon domain={'factchecker.in'} />
    <p>Should never be displayed since the url is incorrect:</p>
    <Favicon domain={'abcde.fgh'} />
  </Story>
</Canvas>

## Args Table

<ArgsTable of={Favicon} />
